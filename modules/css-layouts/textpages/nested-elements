#Nested Elements (Boxes in Boxes)

One key idea with the CSS box model is that child elements display inside the content box of their parents. Consider the following example:

```html
<div class="parent">
  <div class="child">

  </div>
</div>
```
